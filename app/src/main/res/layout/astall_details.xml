<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFF0F0"
    tools:context=".Astall_detailsActivity">

    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"
        android:background="#FF6B6B"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/backButton"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_back_arrow"
            app:tint="@android:color/white"
            android:background="?attr/selectableItemBackgroundBorderless" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Stall Details"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textAlignment="center"/>
    </LinearLayout>

    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/topBar"
        app:layout_constraintBottom_toTopOf="@+id/actionButtonsLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="4dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <ImageView
                    android:id="@+id/stallDetailImage"
                    android:layout_width="match_parent"
                    android:layout_height="200dp"
                    android:scaleType="centerCrop"
                    tools:src="@drawable/stall_sample1"/>

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="20dp">

                    <TextView android:text="Stall Name" style="@style/DetailLabel"/>
                    <TextView android:id="@+id/stallNameValue" style="@style/DetailValue" tools:text="Street Food Corner"/>

                    <TextView android:text="Owner Name" style="@style/DetailLabel" android:layout_marginTop="16dp"/>
                    <TextView android:id="@+id/ownerNameValue" style="@style/DetailValue" tools:text="John Smith"/>

                    <TextView android:text="Email Address" style="@style/DetailLabel" android:layout_marginTop="16dp"/>
                    <TextView android:id="@+id/emailAddressValue" style="@style/DetailValue" tools:text="john.smith@example.com"/>

                    <TextView android:text="FSSAI Number" style="@style/DetailLabel" android:layout_marginTop="16dp"/>
                    <TextView android:id="@+id/fssaiNumberValue" style="@style/DetailValue" tools:text="12345678901234"/>

                    <TextView android:text="Phone Number" style="@style/DetailLabel" android:layout_marginTop="16dp"/>
                    <TextView android:id="@+id/phoneNumberValue" style="@style/DetailValue" tools:text="+1 234-567-8900"/>

                    <TextView android:text="Address" style="@style/DetailLabel" android:layout_marginTop="16dp"/>
                    <TextView android:id="@+id/addressValue" style="@style/DetailValue" tools:text="Block A, University Campus, Street Name, City, State, ZIP"/>

                    <TextView android:text="Date Requested" style="@style/DetailLabel" android:layout_marginTop="16dp"/>
                    <TextView android:id="@+id/dateRequestedValue" style="@style/DetailValue" tools:text="Oct 15, 2025"/>
                </LinearLayout>
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>
    </ScrollView>

    <LinearLayout
        android:id="@+id/actionButtonsLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="@android:color/transparent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/approveButton"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Approve"
            android:textColor="@android:color/white"
            app:backgroundTint="#4CAF50"
            app:cornerRadius="12dp"
            app:icon="@drawable/ic_tick"
            app:iconGravity="textStart"
            app:iconTint="@android:color/white"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/rejectButton"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_marginTop="8dp"
            android:text="Reject"
            android:textColor="@android:color/white"
            app:backgroundTint="#F44336"
            app:cornerRadius="12dp"
            app:icon="@drawable/ic_close"
            app:iconGravity="textStart"
            app:iconTint="@android:color/white"/>
    </LinearLayout>

    <FrameLayout
        android:id="@+id/loadingOverlay"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#B3000000"
        android:clickable="true"
        android:focusable="true"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:visibility="visible">
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>